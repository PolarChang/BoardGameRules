[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python main.py ingest"

[deploy]
startCommand = "uvicorn app:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
